langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_related_formulas
    - node.type.formula
id: node.formula.field_related_formulas
field_name: field_related_formulas
entity_type: node
bundle: formula
label: 'Related formulas (curated)'
description: 'Editor-selected formulas that are traditionally compared, combined, or confused with this one.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      formula: formula
    sort:
      field: title
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
